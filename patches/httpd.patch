Index: src/router/httpd/visuals/menu.c
===================================================================
--- src/router/httpd/visuals/menu.c	(revision HEAD)
+++ src/router/httpd/visuals/menu.c	(working copy)
@@ -511,7 +511,7 @@ EJ_VISIBLE void ej_do_menu(webs_t wp, int argc, char_t **argv)
 				if (!strcmp_pnt(m->menu[i][j], "PPPoE_Server.asp"))
 					goto skip;
 #endif
-#ifdef HAVE_MICRO
+#if defined(HAVE_MICRO) || !defined(HAVE_PPTPD)
 				if (!strcmp_pnt(m->menu[i][j],
 						"PPTP.asp")) // jump over PPTP in
 					// micro build
@@ -538,7 +538,7 @@ EJ_VISIBLE void ej_do_menu(webs_t wp, int argc, char_t **argv)
 				if (!strcmp_pnt(m->menu[i][j], "Upgrade.asp"))
 					goto skip;
 #endif
-#ifdef HAVE_SANSFIL
+#if defined(HAVE_SANSFIL) || !defined(HAVE_HOTSPOT)
 				if (!strcmp_pnt(m->menu[i][j], "Hotspot.asp"))
 					goto skip;
 #endif
